질문을 떠올린 문장: 딕셔너리, Series, 함수는 색인을 대응하여 grouping 한다.

질문1: 색인이 아닌 칼럼값을 대응시킬 순 없나?
답변1: set_index() 이용해서 칼럼값을 색인으로 이동시킨 후, groupby 객체를 생성한다.

질문2: multiindex는 어떻게 처리되나?
답변2: (1) 각 level의 색인 값을 튜플로 묶어 전달하거나, (2) level인자와 해당 level의 색인만 대응시키는 딕셔너리를 전달한다.